# Base de données PostgreSQL locale (via Docker Compose)
DATABASE_URL="postgresql://postgres:postgrespassword@localhost:5434/thermidor_dev?schema=public"

# URL publique de l'application
NEXT_PUBLIC_APP_URL="http://localhost:3022"

# Clé secrète pour le chiffrement des tokens OAuth (mock KMS en dev)
ENCRYPTION_SECRET="super-secret-key-32-chars-long!!"

# Configuration Meta Graph API (Phase test / prod)
META_APP_ID="mock_meta_app_id"
META_APP_SECRET="mock_meta_app_secret"
META_REDIRECT_URI="http://localhost:3022/api/auth/callback/meta"
